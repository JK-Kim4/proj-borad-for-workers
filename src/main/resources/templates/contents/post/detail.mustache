{{>layout/header}}
<main class="container col-10">
    <div class="p-4 p-md-5 mb-4 text-white rounded bg-dark">
        <div class="col-md-6 px-0">
            <p class="lead my-3">공지 사항 및 이벤트 등 캐러샐 추가 영역</p>
            <p class="lead mb-0"><a href="#" class="text-white fw-bold">Continue reading...</a></p>
        </div>
    </div>

    <input type="hidden" id="postId" value="{{postId}}">
    <input type="hidden" id="boardId" value="">
    <input type="hidden" id="memberId" value="">

    <div class="row">
        <div class="d-flex flex-row justify-content-start col-10">
            <h5 id="postTitle"></h5>
        </div>
        <div class="d-flex flex-row justify-content-end col-auto">
            😄<label for="firstName" class="form-label text-center" id="postAuthor"></label>
        </div>
    </div>


    <hr class="my-4">
    <div class="row d-flex justify-content-center">
        <div class="row">
            <div class="d-flex flex-row justify-content-end col-12" style="color:gray;" id="readCount">
                조회수: 0
            </div>
        </div>
        <div class="row mb-4">
            <div class="col-12" id="postContent">
            </div>
            <div class="d-flex flex-row justify-content-center col-12">
                <button class="btn btn-outline-primary" id="recommendButton">추천</button>
                &nbsp;&nbsp;
                <button class="btn btn-outline-danger" id="reportButton">신고</button>
            </div>
        </div>

        <hr class="my-4">

        <!--<div class="row mb-3">
            <div class="col-12">
                댓글 영역
            </div>
        </div>-->

        <div class="row d-flex justify-content-end">
            <div class="col-auto">
                <button class="btn btn-outline-dark" onclick="history.back();">뒤로 가기</button>
            </div>
            <div class="col-auto">
                <button class="btn btn-primary" id="postUpdateButton">수정하기</button>
            </div>
        </div>
    </div>

    <hr class="my-4">


    <script src="/js/board.js"></script>
    <script type="application/javascript">
        $(document).ready(function (){
            let postId = $("#postId").val();
            main.findPostById(postId);
        });
    </script>
</main>
{{>layout/footer}}